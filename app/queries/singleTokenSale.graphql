query IndexSingleTokenSale($id: ID!) {
  tokenSale(id: $id) {
    id
    metadataURI
    presaleId
    startTime
    endTime
    minTotalPayment
    maxTotalPayment
    totalPaymentMade
    salePrice
    paymentToken {
      symbol
      id
    }
    saleToken {
      symbol
      id
    }
  }
}
